# Application configuration
app {
    name = "MyApp"
    version = "1.0"
    server {
        host = "localhost"
        port = 8080
    }
    logging {
        level = "DEBUG"
    }
    nested {
        values = [1, 2, 3]
    }
}